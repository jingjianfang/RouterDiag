RC13 Batch42 - WAN接口约束与抓包布局回归修复

1. WAN自动检测不再接受 br0/br*/lan*/lo 作为WAN接口。
2. 默认路由若指向 br0，仅记录路由异常证据，不把 br0 提升为WAN。
3. 无可信WAN接口时明确显示“未检测到 WAN 接口”，不从任意网口猜测。
4. 终端网口抓包仍允许并固定使用 br0；WAN/主站抓包拒绝 br0/LAN接口。
5. 修复1280响应式重排后“诊断模式/抓包模式/双屏模式”布局控件脱离grid、漂到左上角的问题。
6. 新增WAN接口合法性、br0拒绝、无WAN保持未检测和响应式布局管理回归测试。
